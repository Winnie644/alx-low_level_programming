#include <stdio.h>

/**
 * main- prints alphabets in lower case
 *
 * Return: 0 (success)
 */

int main(void)
{
	char ar;

	for (ar = 'a'; ar <= 'z'; ar++)
	{
	putchar(ar);
	}
	putchar('\n');
	return (0);
}

